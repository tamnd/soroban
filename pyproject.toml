[project]
name = "soroban"
version = "0.1.0"
description = "Learn to train neural networks by hand, one asserted lesson at a time"
requires-python = ">=3.10"
dependencies = ["numpy>=1.26"]

[project.optional-dependencies]
torch = ["torch>=2.4"]
notebook = ["jupyter", "matplotlib", "nbclient", "ipykernel"]

[tool.uv]
package = false
